import React from 'react';
import PropTypes from 'prop-types';
import './Hero.css';

const Hero = () => (
  <div className="Hero">
    Hero Component
  </div>
);

Hero.propTypes = {};

Hero.defaultProps = {};

export default Hero;
